# IO_Projekt
Projekt na IO, by Bartłomiej Gaweł, Wojciech Legierski, Jakub Korona

Start the application with the following commands:

streamlit run app.py     # admin panel
streamlit run verify.py  # worker verification
